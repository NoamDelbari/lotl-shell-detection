__pycache__/
*.pyc
.pytest_cache/
*.bak
*.bak-*

# Re-downloadable caches (build_dataset.py fetches these; scripts/raw/extracted/*.cm
# are vendored and ARE committed)
scripts/raw/*.cm
scripts/raw/*.json
scripts/raw/*.parquet

# Third-party papers read for Ch2/Ch8.3. Redistribution is not ours to grant
# (arXiv licence), so the PDFs stay local and are cited, never vendored.
docs/refs/
