# Baseline and shortcut audit

TF-IDF (char_wb 3-5 grams) + logistic regression, decision threshold 0.5. Generated by `evaluate_baseline.py`, seed 42. This is a deliberately simple model: it exists to expose shortcuts, not to be the project's detector.

## How to read this table

F1 has no absolute scale -- it depends on the prevalence *p* of the malicious class in whatever test set it was measured on. A model that ignores its input and predicts MALICIOUS every time scores **2p/(1 + p)**: 0.400 at p = 0.25, 0.667 at p = 0.50, 0.908 at p = 0.83. So an F1 of 0.99 on an 83%-malicious subset is a *weaker* result than 0.985 on a 25%-malicious one. Two columns make this explicit:

* **F1 - floor** -- raw distance above the do-nothing model, in F1 points. Comparable only against rows of the same prevalence.
* **(F1 - floor)/(1 - floor)** -- the same distance as a fraction of the available headroom: 0 means "no better than always-malicious", 1 means perfect, negative means worse than do-nothing. This is the column to use when prevalences differ.

PR-AUC gets the same treatment: a random ranker's average precision is exactly *p*, reported as **PR-AUC base**. ROC-AUC's baseline is always 0.5 and is not tabulated.

Rows whose prevalence differs from their dataset's full-test row say so in the setting cell.

## Scores

| dataset | setting | test rows | n mal | prevalence p | do-nothing floor 2p/(1+p) | F1 | F1 - floor | (F1 - floor)/(1 - floor) | ROC-AUC | PR-AUC | PR-AUC base (= p) |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| dataset1 | full test (in-distribution) | 3,049 | 762 | 0.2499 | 0.3999 | 0.8975 | +0.4976 | +0.8293 | 0.9833 | 0.9619 | 0.2499 |
| dataset1 | ablation: length-matched 1:1 test subset <br/>*PREVALENCE DIFFERS from the full-test row (0.5000 by construction); only 749/762 attacks (98.3%) could be matched -- same model, subset of the same test set* | 1,498 | 749 | 0.5000 | 0.6667 | 0.9301 | +0.2635 | +0.7904 | 0.9819 | 0.9832 | 0.5000 |
| dataset1 | ablation: addresses stripped (full test) <br/>*same rows and same prevalence as the full-test row* | 3,049 | 762 | 0.2499 | 0.3999 | 0.8990 | +0.4991 | +0.8316 | 0.9828 | 0.9610 | 0.2499 |
| dataset1 | ablation: addresses stripped + length-matched 1:1 <br/>*PREVALENCE DIFFERS from the full-test row (0.5000 by construction); 750/762 attacks (98.4%) matched* | 1,500 | 750 | 0.5000 | 0.6667 | 0.9336 | +0.2669 | +0.8008 | 0.9820 | 0.9834 | 0.5000 |
| dataset2 | full test (in-distribution) | 1,915 | 479 | 0.2501 | 0.4002 | 0.8824 | +0.4822 | +0.8040 | 0.9714 | 0.9365 | 0.2501 |
| dataset2 | ablation: length-matched 1:1 test subset <br/>*PREVALENCE DIFFERS from the full-test row (0.5000 by construction); only 472/479 attacks (98.5%) could be matched -- same model, subset of the same test set* | 944 | 472 | 0.5000 | 0.6667 | 0.9180 | +0.2514 | +0.7541 | 0.9724 | 0.9761 | 0.5000 |
| dataset2 | ablation: addresses stripped (full test) <br/>*same rows and same prevalence as the full-test row* | 1,915 | 479 | 0.2501 | 0.4002 | 0.8843 | +0.4841 | +0.8070 | 0.9720 | 0.9387 | 0.2501 |
| dataset2 | ablation: addresses stripped + length-matched 1:1 <br/>*PREVALENCE DIFFERS from the full-test row (0.5000 by construction); 472/479 attacks (98.5%) matched* | 944 | 472 | 0.5000 | 0.6667 | 0.9180 | +0.2514 | +0.7541 | 0.9708 | 0.9746 | 0.5000 |
| dataset1 -> dataset2 | transfer (all rows of target) <br/>*target's own prevalence, not the source's* | 9,576 | 2,394 | 0.2500 | 0.4000 | 0.5844 | +0.1844 | +0.3073 | 0.8095 | 0.6110 | 0.2500 |
| dataset2 -> dataset1 | transfer (all rows of target) <br/>*target's own prevalence, not the source's* | 15,248 | 3,812 | 0.2500 | 0.4000 | 0.2740 | -0.1260 | -0.2101 | 0.7321 | 0.5401 | 0.2500 |

## Regression probe summary

These probes exist so the problems listed in `KNOWN_ISSUES.md` cannot come back unnoticed. Any FAIL invalidates the scores above it. Full detail, including every threshold and why it is set where it is, is in **Regression probe detail** at the end of this file.

| dataset | probe | verdict |
|---|---|---|
| dataset1 | P1 - selection-regex probe | **PASS** |
| dataset1 | P2/P7 - rows-per-shape probe | **PASS** |
| dataset1 | P3/P5 - single-feature shortcut probes | **INFO** |
| dataset1 | P8 - source-separability probe | **FAIL** |
| dataset2 | P1 - selection-regex probe | **PASS** |
| dataset2 | P2/P7 - rows-per-shape probe | **PASS** |
| dataset2 | P3/P5 - single-feature shortcut probes | **INFO** |
| dataset2 | P8 - source-separability probe | **PASS** |

**FAIL** - 1 probe(s) FAILED: dataset1 / P8 - source-separability probe. Do not quote any score in this report until they pass or the failure is explained in the text.


## Length matching: coverage and whether the lengths actually matched

The length ablation asks whether the model is separating the classes on command length. It can only answer that question over the attacks it could actually pair with a benign command of the same length. Attacks with no partner are dropped (making the subset exactly 1:1); the fraction dropped is the honest limit on what the ablation proves.

| subset | matched / attacks | coverage | tolerance | median len mal (before -> after) | median len benign (before -> after) | mean len after (mal / benign) | median \|delta\| in a pair | max \|delta\| | medians matched? |
|---|---|---:|---|---|---|---|---:|---:|---|
| dataset1/length-matched | 749 / 762 | 98.3% | max(5 chars, 10% of length) | 39 -> 38 | 29 -> 38 | 49.1 / 48.4 | 0 | 25 | yes |
| dataset1/no-addresses+length-matched | 750 / 762 | 98.4% | max(5 chars, 10% of length) | 38 -> 37 | 29 -> 37 | 47.9 / 47.2 | 0 | 25 | yes |
| dataset2/length-matched | 472 / 479 | 98.5% | max(5 chars, 10% of length) | 21 -> 20 | 25 -> 20 | 30.5 / 30.5 | 0 | 25 | yes |
| dataset2/no-addresses+length-matched | 472 / 479 | 98.5% | max(5 chars, 10% of length) | 20 -> 20 | 25 -> 20 | 27.8 / 27.8 | 0 | 25 | yes |

## Per-source behaviour on the test split

Recall for malicious sources, false-positive rate for benign ones, as **k/n with a Wilson 95% interval**. A high headline F1 with weak recall outside the dominant generator means the model learned one source, not the technique. Point estimates from fewer than 30 samples are marked *indicative only*: the smallest source here is `payloads` at n = 15, whose Wilson interval is [0.796, 1.000], i.e. +/-0.102 wide, so a third decimal place is noise.

| dataset | source | class | metric | k / n | rate | Wilson 95% CI | width | flag |
|---|---|---|---|---|---:|---|---:|---|
| dataset1 | `atomic_red_team` | malicious | recall | 120 / 133 | 0.902 | [0.840, 0.942] | 0.102 |  |
| dataset1 | `gtfobins` | malicious | recall | 171 / 189 | 0.905 | [0.854, 0.939] | 0.084 |  |
| dataset1 | `hacktricks` | malicious | recall | 306 / 344 | 0.890 | [0.852, 0.918] | 0.066 |  |
| dataset1 | `payloads` | malicious | recall | 15 / 15 | 1.000 | [0.796, 1.000] | 0.204 | **indicative only (n < 30)** |
| dataset1 | `quasarnix` | malicious | recall | 58 / 58 | 1.000 | [0.938, 1.000] | 0.062 |  |
| dataset1 | `slp` | malicious | recall | 22 / 23 | 0.957 | [0.790, 0.992] | 0.202 | **indicative only (n < 30)** |
| dataset1 | `bash6k` | benign | false-positive rate | 18 / 116 | 0.155 | [0.100, 0.232] | 0.132 |  |
| dataset1 | `bash_instruct` | benign | false-positive rate | 2 / 785 | 0.003 | [0.001, 0.009] | 0.009 |  |
| dataset1 | `linlm` | benign | false-positive rate | 16 / 145 | 0.110 | [0.069, 0.172] | 0.103 |  |
| dataset1 | `nl2bash` | benign | false-positive rate | 25 / 306 | 0.082 | [0.056, 0.118] | 0.062 |  |
| dataset1 | `tldr` | benign | false-positive rate | 27 / 935 | 0.029 | [0.020, 0.042] | 0.022 |  |
| dataset2 | `honeypot` | malicious | recall | 424 / 479 | 0.885 | [0.854, 0.911] | 0.057 |  |
| dataset2 | `bash_history` | benign | false-positive rate | 44 / 1,026 | 0.043 | [0.032, 0.057] | 0.025 |  |
| dataset2 | `commandlinefu` | benign | false-positive rate | 14 / 410 | 0.034 | [0.020, 0.056] | 0.036 |  |

### Leave-one-source-out: is a source's recall memorisation?

Each malicious source is deleted from the TRAINING set, the model is refit on what remains, and the same test rows of that source are scored again. `recall (in train)` is the number from the table above; `recall (source removed)` is what survives when nothing of that source was ever seen. A source that keeps its recall is reachable from the rest of the corpus; a source that collapses was being recognised, not detected. Read this beside the near-duplicate table in the P2/P7 probe: those two together are the evidence for or against 'this corpus is lexically distinct'.

| dataset | source | test n | train rows removed | recall (in train) | recall (source removed) | delta |
|---|---|---:|---:|---|---|---|
| dataset1 | `atomic_red_team` | 133 | 552 | 0.902 [0.840, 0.942] | 0.624 [0.539, 0.702] | -0.278 |
| dataset1 | `gtfobins` | 189 | 768 | 0.905 [0.854, 0.939] | 0.402 [0.335, 0.473] | -0.503 |
| dataset1 | `hacktricks` | 344 | 1,461 | 0.890 [0.852, 0.918] | 0.538 [0.485, 0.590] | -0.352 |
| dataset1 | `payloads` | 15 | 45 | 1.000 [0.796, 1.000] | 0.867 [0.621, 0.963] | -0.133 |
| dataset1 | `quasarnix` | 58 | 146 | 1.000 [0.938, 1.000] | 0.983 [0.909, 0.997] | -0.017 |
| dataset1 | `slp` | 23 | 78 | 0.957 [0.790, 0.992] | 0.957 [0.790, 0.992] | +0.000 |
| dataset2 | `honeypot` | 479 | n/a | n/a | n/a | skipped: removing it leaves train single-class |

Sources holding recall >= 0.90 with themselves removed from training: `quasarnix` (0.983), `slp` (0.957). For those sources the score is not self-memorisation - though it can still be memorisation of a *different* source that resembles them, which is what the near-duplicate table checks.

> **Sources whose recall collapses without themselves in training:** `gtfobins` 0.905 -> 0.402. Their contribution to the headline F1 is recognition of their own corpus, and the headline must not be quoted as evidence that the model generalises to those techniques.


### Pairwise separability of malicious sources (machine-checkable)

One line per pair of malicious sources within a dataset. `OVERLAP` means the two Wilson intervals intersect and **no claim that one source scores better than the other is supported**. Grep this block before writing any comparative sentence.

```
OVERLAP   dataset1 atomic_red_team[0.840,0.942] vs gtfobins[0.854,0.939]
OVERLAP   dataset1 atomic_red_team[0.840,0.942] vs hacktricks[0.852,0.918]
OVERLAP   dataset1 atomic_red_team[0.840,0.942] vs payloads[0.796,1.000]
OVERLAP   dataset1 atomic_red_team[0.840,0.942] vs quasarnix[0.938,1.000]
OVERLAP   dataset1 atomic_red_team[0.840,0.942] vs slp[0.790,0.992]
OVERLAP   dataset1 gtfobins[0.854,0.939] vs hacktricks[0.852,0.918]
OVERLAP   dataset1 gtfobins[0.854,0.939] vs payloads[0.796,1.000]
OVERLAP   dataset1 gtfobins[0.854,0.939] vs quasarnix[0.938,1.000]
OVERLAP   dataset1 gtfobins[0.854,0.939] vs slp[0.790,0.992]
OVERLAP   dataset1 hacktricks[0.852,0.918] vs payloads[0.796,1.000]
SEPARATED dataset1 hacktricks[0.852,0.918] vs quasarnix[0.938,1.000]
OVERLAP   dataset1 hacktricks[0.852,0.918] vs slp[0.790,0.992]
OVERLAP   dataset1 payloads[0.796,1.000] vs quasarnix[0.938,1.000]
OVERLAP   dataset1 payloads[0.796,1.000] vs slp[0.790,0.992]
OVERLAP   dataset1 quasarnix[0.938,1.000] vs slp[0.790,0.992]
```

## Regression probe detail

### P1 - selection-regex probe - dataset1

`RE_MAL_MARKERS` is the keyword filter that used to SELECT Dataset 2's malicious rows. It no longer decides any label (KNOWN_ISSUES P1); it survives only as this permanently reported baseline. Recall here is the circularity measurement: a value near 1.0 means the label is the regex.

| scope | n | prevalence | floor | precision | recall | recall 95% CI | F1 | F1 - floor | false positives |
|---|---:|---:|---:|---:|---:|---|---:|---:|---:|
| whole dataset | 15,248 | 0.250 | 0.4000 | 0.6151 | 0.1002 | [0.091, 0.110] | 0.1723 | -0.2277 | 239 |
| test split | 3,049 | 0.250 | 0.3999 | 0.6853 | 0.1286 | [0.107, 0.154] | 0.2166 | -0.1833 | 45 |

Trained model F1 on the same test split: **0.8975**. Keyword-rule F1: **0.2166**. **The model beats the keyword rule by +0.6810 F1.**

**PASS** - keyword-rule recall on the test split = 0.1286 (98/762); must stay below 0.50 (expected ~0.17 once labels come from provenance rather than the regex).
**PASS** - model F1 minus keyword-rule F1 = +0.6810; must be at least +0.10.

### P2/P7 - rows-per-shape probe - dataset1

`shape()` blanks the volatile parts of a command so that near-identical commands group together, for dedup and for the group-aware split. Pool columns are measured before the MAX_PER_SHAPE cap and are the meaningful ones; built rows/shape is bounded by the cap.

| source | class | rows | shapes | rows/shape | max rows in one shape | pool rows | pool shapes | pool rows/shape |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| `bash6k` | benign | 606 | 606 | 1.00 | 1 | 4,077 | 3,727 | 1.09 |
| `bash_instruct` | benign | 3,935 | 3,935 | 1.00 | 1 | 33,869 | 22,876 | 1.48 |
| `linlm` | benign | 752 | 752 | 1.00 | 1 | 5,429 | 5,323 | 1.02 |
| `nl2bash` | benign | 1,567 | 1,567 | 1.00 | 1 | 10,548 | 9,561 | 1.10 |
| `tldr` | benign | 4,576 | 4,576 | 1.00 | 1 | 29,552 | 29,349 | 1.01 |
| `atomic_red_team` | malicious | 685 | 643 | 1.07 | 2 | 863 | 710 | 1.22 |
| `gtfobins` | malicious | 957 | 917 | 1.04 | 2 | 1,115 | 991 | 1.13 |
| `hacktricks` | malicious | 1,805 | 1,729 | 1.04 | 2 | 2,054 | 1,858 | 1.11 |
| `payloads` | malicious | 60 | 57 | 1.05 | 2 | 69 | 61 | 1.13 |
| `quasarnix` | malicious | 204 | 102 | 2.00 | 2 | 239,638 | 102 | 2349.39 |
| `slp` | malicious | 101 | 94 | 1.07 | 2 | 123 | 101 | 1.22 |

P2's acceptance criterion is `rows-per-shape > 1.5` measured on the source POOL, before dedup. The built column is shown beside it and is NOT the number the criterion is read against: with MAX_PER_SHAPE = 2 it is bounded by 2 and a healthy source still reads ~1.1, so it cannot distinguish a working shape() from a broken one.

| source | pool rows/shape (the criterion) | built rows/shape (capped at 2) |
|---|---:|---:|
| `quasarnix` | 2349.39 | 2.00 |

**PASS** - on the POOL metric, template-generated sources (`honeypot`, `quasarnix`) collapse into shapes.
**PASS** - the dedup cap MAX_PER_SHAPE=2 holds for every source.
**PASS** - 0 shape groups straddle train and test within a label; must be 0, otherwise near-identical commands sit on both sides of the split and the score is memorisation.
**INFO** - 0 shapes occur under BOTH labels (P7); in 0 of those the two labels sit on opposite sides of the split. Reported, not failed: it makes the task harder, not easier - the model sees the structure labelled benign in training and must call it malicious at test.

**Near-duplicate leakage (independent of `shape()`).** The straddle count above is only as good as `shape()`: if a family fails to collapse, the count reads 0 while near-identical rows sit on both sides of the split. So each test row is also compared to its most similar same-source TRAIN row directly, as binary character 4-gram sets under Jaccard similarity. 1.00 means the two commands share every 4-gram. This metric is stricter than the difflib ratio the original finding used: changing one IPv4 octet in a 56-character command scores 0.846 here against difflib's 0.982. So 0.80, not 0.90, is where 'recognisably the same command line' shows up, and both columns are given. The fail line is on the strict column; the loose column carries a watch line. The last column repeats the measurement on difflib's ratio, the metric the original 26.2% QuasarNix finding was made with, so the repair is reported on the ruler that found the bug; it is sampled where the exact computation would be quadratic and too slow, and carries a Wilson interval when it is.

| source | test rows | median sim | p90 sim | max sim | share >= 0.80 | share >= 0.90 | difflib >= 0.90 | flag |
|---|---:|---:|---:|---:|---:|---:|---|---|
| `slp` | 23 | 0.565 | 0.854 | 0.915 | 17.4% | 4.3% | 39.1% [22.2%, 59.2%] | indicative only (n < 30) |
| `gtfobins` | 189 | 0.457 | 0.801 | 0.976 | 11.1% | 1.6% | 19.0% [14.1%, 25.2%] |  |
| `atomic_red_team` | 133 | 0.476 | 0.759 | 0.952 | 6.8% | 1.5% | 19.5% [13.7%, 27.1%] |  |
| `hacktricks` | 344 | 0.369 | 0.737 | 0.906 | 5.8% | 0.6% | 12.5% [9.4%, 16.4%] |  |
| `bash_instruct` | 785 | 0.514 | 0.743 | 0.944 | 3.9% | 0.3% | 23.7% [20.1%, 27.8%] (sampled 476) |  |
| `bash6k` | 116 | 0.271 | 0.611 | 0.917 | 2.6% | 0.9% | 9.5% [5.4%, 16.2%] |  |
| `nl2bash` | 306 | 0.273 | 0.587 | 0.914 | 2.0% | 0.3% | 3.9% [2.3%, 6.7%] |  |
| `tldr` | 935 | 0.292 | 0.591 | 0.914 | 1.1% | 0.3% | 5.1% [3.4%, 7.7%] (sampled 411) |  |
| `linlm` | 145 | 0.263 | 0.557 | 0.765 | 0.0% | 0.0% | 2.1% [0.7%, 5.9%] |  |
| `payloads` | 15 | 0.414 | 0.594 | 0.684 | 0.0% | 0.0% | 6.7% [1.2%, 29.8%] | indicative only (n < 30) |
| `quasarnix` | 58 | 0.411 | 0.525 | 0.653 | 0.0% | 0.0% | 0.0% [0.0%, 6.2%] |  |

**PASS** - no source has more than 10% of its test rows within 0.90 Jaccard of a train row of the same source.
**INFO** - on difflib - the ruler the original finding used - these sources have more than 10% of their test rows at ratio >= 0.90 from a train row: `slp` 39.1%; `gtfobins` 19.0%; `atomic_red_team` 19.5%; `hacktricks` 12.5%; `bash_instruct` 23.7%. This is reported, not failed: at these command lengths difflib rates `less /path/to/input-file` and `zless /path/to/input-file` about 0.95 similar, and those are two different programs, so the number is an upper bound on leakage rather than a measurement of it. What it does mean is that per-source recall for those sources is substantially recall on close variants of training commands, and must not be read as recall on unseen technique. For contrast, this is the metric on which `quasarnix` measured 26.2% before the shape() repair.
**INFO** - `slp` 17.4% (4/23) of test rows are within the LOOSE 0.80 line of a train row, above the 10% watch line. Not a failure - the assertion is on the 0.90 column - but per-source recall for that source is partly recall on variants of commands it has already seen.
**INFO** - `gtfobins` 11.1% (21/189) of test rows are within the LOOSE 0.80 line of a train row, above the 10% watch line. Not a failure - the assertion is on the 0.90 column - but per-source recall for that source is partly recall on variants of commands it has already seen.

Closest surviving pair, `gtfobins` (similarity 0.976):

```
test : latex '\documentclass{article}\newwrite\tempfile\begin{document}\immediate\openout\tempfile=output-file.tex\immediate\write\tempfile{DATA}\immediate\closeout\tempfile\end{document}'
train: pdflatex '\documentclass{article}\newwrite\tempfile\begin{document}\immediate\openout\tempfile=output-file.tex\immediate\write\tempfile{DATA}\immediate\closeout\tempfile\end{document}'
```


### P3/P5 - single-feature shortcut probes - dataset1

A single feature that separates the classes is a shortcut: it says the dataset can be split without understanding the command. Read the `F1 - floor` column, never the raw F1 - an F1 of 0.40 at 25% prevalence is what a model that shouts MALICIOUS at everything already scores, and the first row measures exactly that.

The last row is the acceptance test for the length ablation: on a subset that really is length-matched, a length-only classifier must be uninformative. Read its **ROC-AUC** (0.5 = no signal left); its F1 is not the right readout there, because the probe's threshold was fitted at p = 0.25 and the subset is p = 0.50.

| probe | scope | n | n mal | prevalence p | floor | precision | recall | F1 | F1 - floor | (F1 - floor)/(1 - floor) | ROC-AUC |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| do-nothing (always malicious) | test split | 3,049 | 762 | 0.250 | 0.3999 | 0.2499 | 1.0000 | 0.3999 | +0.0000 | +0.0000 | n/a |
| 'contains an IPv4' rule | whole dataset | 15,248 | 3,812 | 0.250 | 0.4000 | 0.8073 | 0.0813 | 0.1478 | -0.2522 | -0.4204 | n/a |
| 'contains an IPv4' rule | test split | 3,049 | 762 | 0.250 | 0.3999 | 0.8605 | 0.0971 | 0.1745 | -0.2254 | -0.3755 | n/a |
| length-only classifier | test split | 3,049 | 762 | 0.250 | 0.3999 | 0.3429 | 0.4409 | 0.3858 | -0.0141 | -0.0235 | 0.6112 |
| length-only classifier | length-matched 1:1 subset (p = 0.5000) | 1,498 | 749 | 0.500 | 0.6667 | 0.5000 | 0.4312 | 0.4631 | -0.2036 | -0.6108 | 0.5005 |

**INFO** - single-feature probes are descriptive, not pass/fail: they size each shortcut. Compare each `F1 - floor` with the trained model's `F1 - floor` on the same rows in the scores table.

### P8 - source-separability probe - dataset1

**(a) Which benign source did this command come from?** 5 benign sources, same TF-IDF + LogReg recipe, same train/test split, trained on the benign part of train (capped at 3,000 rows per source) and scored on the 2,287 benign rows of test. The malicious/benign label is never used.

| true \ predicted | `bash6k` | `bash_instruct` | `linlm` | `nl2bash` | `tldr` | test n | recall |
|---|---:|---:|---:|---:|---:|---:|---:|
| `bash6k` | 79 | 9 | 7 | 14 | 7 | 116 | 0.681 |
| `bash_instruct` | 15 | 747 | 8 | 12 | 3 | 785 | 0.952 |
| `linlm` | 8 | 5 | 93 | 6 | 33 | 145 | 0.641 |
| `nl2bash` | 16 | 6 | 6 | 271 | 7 | 306 | 0.886 |
| `tldr` | 13 | 8 | 57 | 12 | 845 | 935 | 0.904 |

benign-source macro-F1 = **0.7965** on n = 2,287 test rows over k = 5 classes, largest class 40.9%. Always predicting that largest class scores (2q/(1+q))/k = **0.1161**, so the headroom-normalised value is **0.7697** (0 = no better than that, 1 = perfect). The normalised column is the one comparable with the 2-class pairs in (b); the raw column is not.

**(b) One source against one source.** `mal-vs-ben` is the detection task restricted to a single source on each side. `ben-vs-ben` involves no maliciousness at all - it is pure collection-style separability, and it is the control that tells you how to read the mal-vs-ben column.

Every row carries the majority share of its own test set, the do-nothing macro-F1 floor q/(1+q) that follows from it, and the headroom-normalised score. A pair at 0.95 against a floor of 0.49 and a pair at 0.95 against a floor of 0.33 are not the same result.

| family | source A | source B | test n | majority q | floor | macro-F1 | above floor | normalised |
|---|---|---|---:|---:|---:|---:|---:|---:|
| mal-vs-ben | `atomic_red_team` | `bash6k` | 249 | 0.534 | 0.3482 | 0.8992 | +0.5510 | 0.8453 |
| mal-vs-ben | `atomic_red_team` | `bash_instruct` | 918 | 0.855 | 0.4610 | 0.9848 | +0.5238 | 0.9717 |
| mal-vs-ben | `atomic_red_team` | `linlm` | 278 | 0.522 | 0.3428 | 0.9239 | +0.5812 | 0.8843 |
| mal-vs-ben | `atomic_red_team` | `nl2bash` | 439 | 0.697 | 0.4107 | 0.9626 | +0.5518 | 0.9365 |
| mal-vs-ben | `atomic_red_team` | `tldr` | 1,068 | 0.875 | 0.4668 | 0.9608 | +0.4940 | 0.9266 |
| mal-vs-ben | `gtfobins` | `bash6k` | 305 | 0.620 | 0.3826 | 0.9420 | +0.5594 | 0.9061 |
| mal-vs-ben | `gtfobins` | `bash_instruct` | 974 | 0.806 | 0.4463 | 0.9764 | +0.5301 | 0.9573 |
| mal-vs-ben | `gtfobins` | `linlm` | 334 | 0.566 | 0.3614 | 0.9160 | +0.5546 | 0.8684 |
| mal-vs-ben | `gtfobins` | `nl2bash` | 495 | 0.618 | 0.3820 | 0.9611 | +0.5791 | 0.9370 |
| mal-vs-ben | `gtfobins` | `tldr` | 1,124 | 0.832 | 0.4541 | 0.9451 | +0.4910 | 0.8994 |
| mal-vs-ben | `hacktricks` | `bash6k` | 460 | 0.748 | 0.4279 | 0.8915 | +0.4637 | 0.8104 |
| mal-vs-ben | `hacktricks` | `bash_instruct` | 1,129 | 0.695 | 0.4101 | 0.9833 | +0.5731 | 0.9716 |
| mal-vs-ben | `hacktricks` | `linlm` | 489 | 0.703 | 0.4130 | 0.8748 | +0.4618 | 0.7866 |
| mal-vs-ben | `hacktricks` | `nl2bash` | 650 | 0.529 | 0.3461 | 0.9428 | +0.5967 | 0.9125 |
| mal-vs-ben | `hacktricks` | `tldr` | 1,279 | 0.731 | 0.4223 | 0.9370 | +0.5146 | 0.8909 |
| mal-vs-ben | `payloads` | `bash6k` | 131 | 0.885 | 0.4696 | 0.9418 | +0.4722 | 0.8903 |
| mal-vs-ben | `payloads` | `bash_instruct` | 800 | 0.981 | 0.4953 | 0.9824 | +0.4872 | 0.9652 |
| mal-vs-ben | `payloads` | `linlm` | 160 | 0.906 | 0.4754 | 0.9609 | +0.4855 | 0.9254 |
| mal-vs-ben | `payloads` | `nl2bash` | 321 | 0.953 | 0.4880 | 0.9301 | +0.4420 | 0.8634 |
| mal-vs-ben | `payloads` | `tldr` | 950 | 0.984 | 0.4960 | 0.9638 | +0.4677 | 0.9281 |
| mal-vs-ben | `quasarnix` | `bash6k` | 174 | 0.667 | 0.4000 | 0.9936 | +0.5936 | 0.9893 |
| mal-vs-ben | `quasarnix` | `bash_instruct` | 843 | 0.931 | 0.4822 | 1.0000 | +0.5178 | 1.0000 |
| mal-vs-ben | `quasarnix` | `linlm` | 203 | 0.714 | 0.4167 | 0.9940 | +0.5773 | 0.9897 |
| mal-vs-ben | `quasarnix` | `nl2bash` | 364 | 0.841 | 0.4567 | 0.9949 | +0.5382 | 0.9906 |
| mal-vs-ben | `quasarnix` | `tldr` | 993 | 0.942 | 0.4850 | 1.0000 | +0.5150 | 1.0000 |
| mal-vs-ben | `slp` | `bash6k` | 139 | 0.835 | 0.4549 | 0.9479 | +0.4930 | 0.9044 |
| mal-vs-ben | `slp` | `bash_instruct` | 808 | 0.972 | 0.4928 | 1.0000 | +0.5072 | 1.0000 |
| mal-vs-ben | `slp` | `linlm` | 168 | 0.863 | 0.4633 | 1.0000 | +0.5367 | 1.0000 |
| mal-vs-ben | `slp` | `nl2bash` | 329 | 0.930 | 0.4819 | 0.9077 | +0.4258 | 0.8218 |
| mal-vs-ben | `slp` | `tldr` | 958 | 0.976 | 0.4939 | 1.0000 | +0.5061 | 1.0000 |
| ben-vs-ben | `bash6k` | `bash_instruct` | 901 | 0.871 | 0.4656 | 0.8963 | +0.4307 | 0.8059 |
| ben-vs-ben | `bash6k` | `linlm` | 261 | 0.556 | 0.3571 | 0.8411 | +0.4839 | 0.7528 |
| ben-vs-ben | `bash6k` | `nl2bash` | 422 | 0.725 | 0.4203 | 0.8161 | +0.3958 | 0.6828 |
| ben-vs-ben | `bash6k` | `tldr` | 1,051 | 0.890 | 0.4708 | 0.9195 | +0.4487 | 0.8478 |
| ben-vs-ben | `bash_instruct` | `linlm` | 930 | 0.844 | 0.4577 | 0.9499 | +0.4922 | 0.9076 |
| ben-vs-ben | `bash_instruct` | `nl2bash` | 1,091 | 0.720 | 0.4184 | 0.9718 | +0.5533 | 0.9514 |
| ben-vs-ben | `bash_instruct` | `tldr` | 1,720 | 0.544 | 0.3522 | 0.9877 | +0.6355 | 0.9810 |
| ben-vs-ben | `linlm` | `nl2bash` | 451 | 0.678 | 0.4042 | 0.9193 | +0.5150 | 0.8645 |
| ben-vs-ben | `linlm` | `tldr` | 1,080 | 0.866 | 0.4640 | 0.8158 | +0.3518 | 0.6564 |
| ben-vs-ben | `nl2bash` | `tldr` | 1,241 | 0.753 | 0.4297 | 0.9675 | +0.5378 | 0.9430 |

5 pair(s) score an exact 1.0000 - every test row assigned to the right pile, no exceptions: `quasarnix` vs `bash_instruct`, `quasarnix` vs `tldr`, `slp` vs `bash_instruct`, `slp` vs `linlm`, `slp` vs `tldr`. An exact 1.0000 is not 'very separable', it is a giveaway token: some feature present in one pile and absent from the other decides every row, and a model can reach those rows without learning anything about attacks. For `quasarnix` the token is identified: every one of its rows contains an IPv4 literal, against 0.00%-1.94% of each benign Dataset 1 source (`ipv4_share_by_source` in stats.json).

mean mal-vs-ben macro-F1 = **0.9573** raw (min 0.8748, max 1.0000), **0.9258** normalised, over 30 pairs.
mean ben-vs-ben macro-F1 = **0.9085** raw (min 0.8158, max 0.9877), **0.8393** normalised, over 10 pairs.

**Style gap** (mal-vs-ben minus ben-vs-ben) = **+0.0488** on the raw scale, **+0.0864** normalised. The two families do not share a ruler - their pairs have different class balances, so different floors (mal-vs-ben mean floor 0.4376, ben-vs-ben mean floor 0.4240) - and subtracting two raw means across different floors measures partly the difference in floors. The normalised gap is the one to read.

**How to read this, and what a bad result costs the project.** This probe never looks at the malicious/benign label; it asks only whether a command carries a fingerprint of the pile it was collected from. Both datasets are built provenance-matched (attack side and normal side drawn from the same KIND of source) precisely so the classes cannot be told apart on collection style, and that defence is worth exactly what this number says. Macro-F1 above 0.90 raw, or above 0.85 once its own do-nothing floor is divided out, means 'which pile is this' is trivially learnable, the provenance-matching argument is design intent rather than a verified property, and the headline F1 must be presented as an upper bound that is partly corpus identification. Concretely, a bad result means: (1) the headline F1 may not be quoted without the cross-dataset transfer result beside it, since transfer is then the only evidence of generalisation; (2) DATA_CARD's provenance-matching paragraph must be downgraded from a property to an intention; (3) the ablations become load-bearing rather than confirmatory. A SMALL style gap is the reassuring case: it says telling an attack pile from a benign pile is no easier than telling two benign piles apart, so the detection score is not merely pile identification. Dataset 2 is the one to watch - its benign side is one Linux hobbyist's history (git, sudo nano, apt) and its malicious side is botnet traffic (busybox, tftp, chmod +x), so vocabulary alone could carry the score.

**PASS** - benign-source macro-F1 = 0.7965 raw (line 0.90) and 0.7697 normalised against its floor 0.1161 (line 0.85); k = 5 classes, n = 2,287. Both must hold.
**INFO** - macro-F1 0.7965 is in the 0.75-0.90 concern band: benign sources are already largely identifiable by style alone.
**FAIL** - mean pairwise benign-vs-benign macro-F1 = 0.9085 raw (line 0.90) and 0.8393 normalised (line 0.85), over 10 pair(s). Both must hold. A FAIL here is a standing property of the source corpora, not a regression introduced by the last build - it does not go away by rerunning.
**INFO** - bootstrap over the 10 pairs (percentile, 2,000 resamples) puts the mean in [0.8713, 0.9452] - the 0.90 line falls INSIDE that interval, so the verdict is BORDERLINE: the sign of the margin (+0.0085) is not resolved by 10 pairs and should not be reported as a clean result in either direction.
**INFO** - normalised style gap +0.0864 is small (< 0.10): separating the attack pile from a benign pile is barely harder than separating two benign piles, so little of the headline score can be attributed to maliciousness specifically.

### P1 - selection-regex probe - dataset2

`RE_MAL_MARKERS` is the keyword filter that used to SELECT Dataset 2's malicious rows. It no longer decides any label (KNOWN_ISSUES P1); it survives only as this permanently reported baseline. Recall here is the circularity measurement: a value near 1.0 means the label is the regex.

| scope | n | prevalence | floor | precision | recall | recall 95% CI | F1 | F1 - floor | false positives |
|---|---:|---:|---:|---:|---:|---|---:|---:|---:|
| whole dataset | 9,576 | 0.250 | 0.4000 | 0.7121 | 0.3275 | [0.309, 0.347] | 0.4486 | +0.0486 | 317 |
| test split | 1,915 | 0.250 | 0.4002 | 0.7120 | 0.2839 | [0.245, 0.326] | 0.4060 | +0.0058 | 55 |

Trained model F1 on the same test split: **0.8824**. Keyword-rule F1: **0.4060**. **The model beats the keyword rule by +0.4764 F1.**

**PASS** - keyword-rule recall on the test split = 0.2839 (136/479); must stay below 0.50 (expected ~0.17 once labels come from provenance rather than the regex).
**PASS** - model F1 minus keyword-rule F1 = +0.4764; must be at least +0.10.

### P2/P7 - rows-per-shape probe - dataset2

`shape()` blanks the volatile parts of a command so that near-identical commands group together, for dedup and for the group-aware split. Pool columns are measured before the MAX_PER_SHAPE cap and are the meaningful ones; built rows/shape is bounded by the cap.

| source | class | rows | shapes | rows/shape | max rows in one shape | pool rows | pool shapes | pool rows/shape |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| `bash_history` | benign | 5,027 | 5,027 | 1.00 | 1 | 50,978 | 45,384 | 1.12 |
| `commandlinefu` | benign | 2,155 | 2,155 | 1.00 | 1 | 7,025 | 6,706 | 1.05 |
| `honeypot` | malicious | 2,394 | 2,212 | 1.08 | 2 | 407,442 | 2,455 | 165.96 |

P2's acceptance criterion is `rows-per-shape > 1.5` measured on the source POOL, before dedup. The built column is shown beside it and is NOT the number the criterion is read against: with MAX_PER_SHAPE = 2 it is bounded by 2 and a healthy source still reads ~1.1, so it cannot distinguish a working shape() from a broken one.

| source | pool rows/shape (the criterion) | built rows/shape (capped at 2) |
|---|---:|---:|
| `honeypot` | 165.96 | 1.08 |

**PASS** - on the POOL metric, template-generated sources (`honeypot`, `quasarnix`) collapse into shapes.
**PASS** - the dedup cap MAX_PER_SHAPE=2 holds for every source.
**PASS** - 0 shape groups straddle train and test within a label; must be 0, otherwise near-identical commands sit on both sides of the split and the score is memorisation.
**INFO** - 0 shapes occur under BOTH labels (P7); in 0 of those the two labels sit on opposite sides of the split. Reported, not failed: it makes the task harder, not easier - the model sees the structure labelled benign in training and must call it malicious at test.

**Near-duplicate leakage (independent of `shape()`).** The straddle count above is only as good as `shape()`: if a family fails to collapse, the count reads 0 while near-identical rows sit on both sides of the split. So each test row is also compared to its most similar same-source TRAIN row directly, as binary character 4-gram sets under Jaccard similarity. 1.00 means the two commands share every 4-gram. This metric is stricter than the difflib ratio the original finding used: changing one IPv4 octet in a 56-character command scores 0.846 here against difflib's 0.982. So 0.80, not 0.90, is where 'recognisably the same command line' shows up, and both columns are given. The fail line is on the strict column; the loose column carries a watch line. The last column repeats the measurement on difflib's ratio, the metric the original 26.2% QuasarNix finding was made with, so the repair is reported on the ruler that found the bug; it is sampled where the exact computation would be quadratic and too slow, and carries a Wilson interval when it is.

| source | test rows | median sim | p90 sim | max sim | share >= 0.80 | share >= 0.90 | difflib >= 0.90 | flag |
|---|---:|---:|---:|---:|---:|---:|---|---:|
| `honeypot` | 479 | 0.429 | 0.765 | 0.988 | 5.8% | 2.1% | 24.0% [20.4%, 28.0%] |  |
| `bash_history` | 1,026 | 0.300 | 0.646 | 1.000 | 2.7% | 1.0% | 6.1% [4.1%, 9.1%] (sampled 374) |  |
| `commandlinefu` | 410 | 0.169 | 0.363 | 0.803 | 0.2% | 0.0% | 1.2% [0.5%, 2.8%] |  |

**PASS** - no source has more than 10% of its test rows within 0.90 Jaccard of a train row of the same source.
**INFO** - on difflib - the ruler the original finding used - these sources have more than 10% of their test rows at ratio >= 0.90 from a train row: `honeypot` 24.0%. This is reported, not failed: at these command lengths difflib rates `less /path/to/input-file` and `zless /path/to/input-file` about 0.95 similar, and those are two different programs, so the number is an upper bound on leakage rather than a measurement of it. What it does mean is that per-source recall for those sources is substantially recall on close variants of training commands, and must not be read as recall on unseen technique. For contrast, this is the metric on which `quasarnix` measured 26.2% before the shape() repair.

Closest surviving pair, `bash_history` (similarity 1.000):

```
test : [goalador@gatanda hhneuauf.de]$ [goalador@gatanda hhneuauf.de]$ [goalador@gatanda hhneuauf.de]$ da hhneuauf.de]$ [goalador@gatanda hhneuauf.de]$ [CHG] Controller C8:F7:33:33:E2:F7 UUIDs: 0000110b-0000
train: [goalador@gatanda hhneuauf.de]$ [goalador@gatanda hhneuauf.de]$ [goalador@gatanda hhneuauf.de]$ [goalador@gatanda hhneuauf.de]$ [goalador@gatanda hhneuauf.de]$ da hhneuauf.de]$ [goalador@gatanda hhneu
```


### P3/P5 - single-feature shortcut probes - dataset2

A single feature that separates the classes is a shortcut: it says the dataset can be split without understanding the command. Read the `F1 - floor` column, never the raw F1 - an F1 of 0.40 at 25% prevalence is what a model that shouts MALICIOUS at everything already scores, and the first row measures exactly that.

The last row is the acceptance test for the length ablation: on a subset that really is length-matched, a length-only classifier must be uninformative. Read its **ROC-AUC** (0.5 = no signal left); its F1 is not the right readout there, because the probe's threshold was fitted at p = 0.25 and the subset is p = 0.50.

| probe | scope | n | n mal | prevalence p | floor | precision | recall | F1 | F1 - floor | (F1 - floor)/(1 - floor) | ROC-AUC |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| do-nothing (always malicious) | test split | 1,915 | 479 | 0.250 | 0.4002 | 0.2501 | 1.0000 | 0.4002 | -0.0000 | -0.0000 | n/a |
| 'contains an IPv4' rule | whole dataset | 9,576 | 2,394 | 0.250 | 0.4000 | 0.6109 | 0.0564 | 0.1033 | -0.2967 | -0.4946 | n/a |
| 'contains an IPv4' rule | test split | 1,915 | 479 | 0.250 | 0.4002 | 0.5952 | 0.0522 | 0.0960 | -0.3042 | -0.5071 | n/a |
| length-only classifier | test split | 1,915 | 479 | 0.250 | 0.4002 | 0.2598 | 0.7035 | 0.3795 | -0.0207 | -0.0344 | 0.5703 |
| length-only classifier | length-matched 1:1 subset (p = 0.5000) | 944 | 472 | 0.500 | 0.6667 | 0.5000 | 0.7140 | 0.5881 | -0.0785 | -0.2356 | 0.5041 |

**INFO** - single-feature probes are descriptive, not pass/fail: they size each shortcut. Compare each `F1 - floor` with the trained model's `F1 - floor` on the same rows in the scores table.

### P8 - source-separability probe - dataset2

**(a) Which benign source did this command come from?** 2 benign sources, same TF-IDF + LogReg recipe, same train/test split, trained on the benign part of train (capped at 3,000 rows per source) and scored on the 1,436 benign rows of test. The malicious/benign label is never used.

| true \ predicted | `bash_history` | `commandlinefu` | test n | recall |
|---|---:|---:|---:|---:|
| `bash_history` | 956 | 70 | 1,026 | 0.932 |
| `commandlinefu` | 50 | 360 | 410 | 0.878 |

benign-source macro-F1 = **0.8990** on n = 1,436 test rows over k = 2 classes, largest class 71.4%. Always predicting that largest class scores (2q/(1+q))/k = **0.4167**, so the headroom-normalised value is **0.8269** (0 = no better than that, 1 = perfect). The normalised column is the one comparable with the 2-class pairs in (b); the raw column is not. Note that with exactly two benign sources this multiclass problem IS the single ben-vs-ben pair in (b): the two statistics below are one fit reported twice, not two pieces of evidence.

**(b) One source against one source.** `mal-vs-ben` is the detection task restricted to a single source on each side. `ben-vs-ben` involves no maliciousness at all - it is pure collection-style separability, and it is the control that tells you how to read the mal-vs-ben column.

Every row carries the majority share of its own test set, the do-nothing macro-F1 floor q/(1+q) that follows from it, and the headroom-normalised score. A pair at 0.95 against a floor of 0.49 and a pair at 0.95 against a floor of 0.33 are not the same result.

| family | source A | source B | test n | majority q | floor | macro-F1 | above floor | normalised |
|---|---|---|---:|---:|---:|---:|---:|---:|
| mal-vs-ben | `honeypot` | `bash_history` | 1,505 | 0.682 | 0.4054 | 0.9184 | +0.5130 | 0.8628 |
| mal-vs-ben | `honeypot` | `commandlinefu` | 889 | 0.539 | 0.3501 | 0.9357 | +0.5855 | 0.9010 |
| ben-vs-ben | `bash_history` | `commandlinefu` | 1,436 | 0.714 | 0.4167 | 0.8990 | +0.4823 | 0.8269 |

mean mal-vs-ben macro-F1 = **0.9270** raw (min 0.9184, max 0.9357), **0.8819** normalised, over 2 pairs.
mean ben-vs-ben macro-F1 = **0.8990** raw (min 0.8990, max 0.8990), **0.8269** normalised, over 1 pairs.

**Style gap** (mal-vs-ben minus ben-vs-ben) = **+0.0280** on the raw scale, **+0.0550** normalised. The two families do not share a ruler - their pairs have different class balances, so different floors (mal-vs-ben mean floor 0.3778, ben-vs-ben mean floor 0.4167) - and subtracting two raw means across different floors measures partly the difference in floors. The normalised gap is the one to read.

**How to read this, and what a bad result costs the project.** This probe never looks at the malicious/benign label; it asks only whether a command carries a fingerprint of the pile it was collected from. Both datasets are built provenance-matched (attack side and normal side drawn from the same KIND of source) precisely so the classes cannot be told apart on collection style, and that defence is worth exactly what this number says. Macro-F1 above 0.90 raw, or above 0.85 once its own do-nothing floor is divided out, means 'which pile is this' is trivially learnable, the provenance-matching argument is design intent rather than a verified property, and the headline F1 must be presented as an upper bound that is partly corpus identification. Concretely, a bad result means: (1) the headline F1 may not be quoted without the cross-dataset transfer result beside it, since transfer is then the only evidence of generalisation; (2) DATA_CARD's provenance-matching paragraph must be downgraded from a property to an intention; (3) the ablations become load-bearing rather than confirmatory. A SMALL style gap is the reassuring case: it says telling an attack pile from a benign pile is no easier than telling two benign piles apart, so the detection score is not merely pile identification. Dataset 2 is the one to watch - its benign side is one Linux hobbyist's history (git, sudo nano, apt) and its malicious side is botnet traffic (busybox, tftp, chmod +x), so vocabulary alone could carry the score.

**PASS** - benign-source macro-F1 = 0.8990 raw (line 0.90) and 0.8269 normalised against its floor 0.4167 (line 0.85); k = 2 classes, n = 1,436. Both must hold.
**INFO** - macro-F1 0.8990 is in the 0.75-0.90 concern band: benign sources are already largely identifiable by style alone.
**PASS** - mean pairwise benign-vs-benign macro-F1 = 0.8990 raw (line 0.90) and 0.8269 normalised (line 0.85), over 1 pair(s). Both must hold. A FAIL here is a standing property of the source corpora, not a regression introduced by the last build - it does not go away by rerunning.
**INFO** - that mean is a single pair: this dataset has only two benign sources, so there is no spread to report and the verdict rests on one fit.
**INFO** - normalised style gap +0.0550 is small (< 0.10): separating the attack pile from a benign pile is barely harder than separating two benign piles, so little of the headline score can be attributed to maliciousness specifically.

